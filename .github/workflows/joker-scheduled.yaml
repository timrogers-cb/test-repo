name: Joker Agent - Scheduled Hourly

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

permissions:
  issues: write

jobs:
  trigger-joker-agent:
    name: Trigger Joker Agent
    runs-on: ubuntu-latest
    steps:
      - name: Create issue to trigger Joker agent
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh issue create \
            --repo "${{ github.repository }}" \
            --title "Scheduled joker run - $(date -u '+%Y-%m-%d %H:%M UTC')" \
            --body "This issue was automatically created by the scheduled workflow to trigger the joker agent." \
            --assignee "copilot"
